apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: debasish-portfolio-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: debasish.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: debasish-portfolio-service
            port:
              number: 80
